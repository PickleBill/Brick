# tooling / verification artifacts — not part of the static site
node_modules/
tools/screens/
tools/videos/
package-lock.json

# agent worktrees (parallel build sessions) — never part of the site
.claude/worktrees/
