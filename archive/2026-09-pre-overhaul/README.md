# Before the Sept 2026 overhaul (frozen 2026-09-25, from main@04cf28e)

Bill's rule (2026-09-25): every major visual change keeps the old version somewhere easy to
reach, with a clear "here's what it looked like / here's what it changed to."

## What's here
| File | What it was |
|------|-------------|
| `index.html` + `home.js` | The home page before the overhaul (7 sections, 6-photo card, website-screenshot proof cards). |
| `operator-card.js` | The v6.5 six-facet identity card, renamed to `<operator-card-v65>` so it can sit next to the live card. |
| `work.html` | The proof page ("Receipts, not slides") before the rework. |
| `climb.html` | The Story page before the Courtana copy change. |
| `resume-v2.html`, `resume.html` | The two résumé pages the site linked before the new `resume/` page. |
| `screens/*.before.jpg` | Full-page screenshots, desktop (1440px) and mobile (375px). |

## How to compare
- **Open an old page:** `picklebill.github.io/Brick/archive/2026-09-pre-overhaul/index.html` (and `work.html`, `climb.html`, `resume-v2.html`). Pages here link to each other, so you can click around the old site.
- **The card, old vs new, side by side:** `picklebill.github.io/Brick/playground.html`.
- **Side by side:** `compare.html` pairs every before/after screenshot (desktop and 375px toggle).
- **Screenshots:** `screens/*.before.jpg` (frozen 2026-09-25) and `screens/*.after.jpg` (after the build + two red-team rounds).

These copies are `noindex` and never edited, except to point asset paths at `../../assets/`.
