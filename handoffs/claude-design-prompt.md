# Claude Design prompt — Bill Bricker site (paste this into Claude Design)

> Copy everything in the box below into Claude Design. It carries the goal, the hierarchy, the design
> system, and the things we struggled with, so Claude Design can drive the **visual merge** of my two sites.

---

**I'm merging two versions of my personal site into one and need design direction + inspiration.**

**Who I am:** Bill Bricker — an **AI-Forward Sales & Partnerships leader**. The pitch: I'm an expert
closer who *also* builds with AI. I founded Dreamship and **closed Google as a partner in year one**
(a first-of-its-kind deal that opened a $45M+ ad-spend channel), led it to **$35M+ peak revenue**,
and now build AI-native workflows (40+ apps) at Courtana. Target role: GTM/partnerships at a frontier
AI lab. The site itself is the proof ("sells AND builds").

**The two sites to merge into one:**
- **Original (rich):** `picklebill.github.io/Brick/#top` — fully built, good flow, scroll-spy nav,
  a flip identity card, a live AI terminal, lots of components. More "about me / builder".
- **Sales Dossier (focused):** `picklebill.github.io/Brick/sales/` — simpler, sales-first, the
  Dreamship/Google deal as the centerpiece, a partner-logo "constellation". This is the direction I want.

**Goal:** ONE cohesive, **sales-first** site that keeps the best of both.

**My hierarchy objectives (most important):**
1. **Lead with the close** (Dreamship → Google → revenue). The AI-building is the *support act /
   exploratory layer*, never the headline.
2. **Dead-simple first view, depth on demand** — don't front-load everything.
3. **Mobile must equal desktop** — same content, just reflowed. No hiding things on phones.
4. **Keep the energy** — color + motion are the brand; a fully-muted version felt dead.

**Design system to honor:**
- Near-black background `#08090a`; a signature spectrum gradient **green `#5be8a0` → cyan `#36c6e0` →
  violet `#8b7cf0` → coral `#ff8a6b`**; glass cards (subtle white fill, soft borders, 22px radius);
  tasteful glow/sheen and motion.
- Type: **Bricolage Grotesque** (display), **Hanken Grotesk** (body), **JetBrains Mono** (labels/terminal).
- Anti-pattern: generic "AI-made" interfaces. Use the spectrum **sparingly** (a hero accent or two), not
  on everything.

**Signature components (keep/refine):** a flip identity card (front = a personal photo; back = my
operator identity as text); a **partner-logo constellation** (Google/Microsoft/Stripe/PayPal/Meta/IBM…);
a **live AI terminal** you can ask about me; a Dreamship/Google **proof centerpiece**; a 20-year
**career-climb** story.

**What we kept struggling with — please help here:**
- The **hero + identity card hierarchy** — getting the "expert closer, and also a builder" balance to
  read instantly without clutter.
- The **section flow** for a 6-second skim by a hiring manager → then deeper proof on demand.
- Keeping it **alive but not slop** (lots of gradients/animation is tempting and tips into "AI-made").

**What I want from you:** propose a **unified sales-first design** — hero treatment, section order, and how
to fold the original site's richer components into the simpler sales frame. Give me 2–3 directions with
visual inspiration, then we'll refine one. Mobile-first.

---

*(Context for next coding session: Claude Design output → feed back to Impeccable in the repo to execute.
The repo's `DESIGN.md` + `PRODUCT.md` capture the tokens and strategy.)*
