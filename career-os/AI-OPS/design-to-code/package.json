{
  "name": "design-to-code-watcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "INERT scaffold: Claude Design handoff (labeled issue) -> draft PR. Needs ANTHROPIC_API_KEY. Verify SDK API + model IDs before first run.",
  "scripts": {
    "start": "node run_design_to_code.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0"
  }
}
