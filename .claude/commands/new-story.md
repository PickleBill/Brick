---
description: Structure a raw answer/transcript into a content/stories entry
argument-hint: <topic>
---
Take Bill's raw input (voice-memo transcript or bullet dump) on "$ARGUMENTS" and structure it into a `content/stories/` entry, following Brick's content-first architecture.

- Extract every number/claim and confirm each is in `content/FACTS.md`. If a number isn't in the ledger, flag it (⚠️) and list it for addition — never invent or assume a value.
- Assign publicity tags (public / blur / private) consistent with surrounding content. Default sensitive territory (health / The Long Walk) to discoverable-not-headline unless Bill has set otherwise.
- Keep the structuring faithful to Bill's words — structure, don't embellish.

Show the proposed story entry and a list of any facts that must be added to the ledger before this can ship.
